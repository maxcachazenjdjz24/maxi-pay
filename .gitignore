# Dependencias y compilación
node_modules/
dist/
*.tsbuildinfo

# Estado del agente en ejecución (nunca debe subirse — contiene la wallet
# cifrada, la base de datos con memoria/historial, y configuración con
# posibles claves de API)
.automaton/
*.db
*.db-wal
*.db-shm

# Wallets, bases de datos y secretos (NUNCA versionar datos de usuarios ni claves)
data/
data/*
registered_users.json
wallet.json
wallets/
*.key
*.pem
*.db
*.db-wal
*.db-shm
.env
.env.*
.env.local

# Sistema operativo / editor
.DS_Store
*.log
.vscode/
.idea/

